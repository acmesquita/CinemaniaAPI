<%= form_for(sessao) do |f| %>
  <% if sessao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sessao.errors.count, "error") %> prohibited this sessao from being saved:</h2>

      <ul>
      <% sessao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :inicio %>
    <%= f.date_select :inicio %>
  </div>

  <div class="field">
    <%= f.label :dublado %>
    <%= f.check_box :dublado %>
  </div>

  <div class="field">
    <%= f.label :tipo_exibicao %>
    <%= f.text_field :tipo_exibicao %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
